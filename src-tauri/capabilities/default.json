{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "TraceLab main window minimum capability set",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
